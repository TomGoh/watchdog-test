# Watchdog 测试用例与执行指南

本文档由 `test_unit_table.xlsx` 整理而来，便于在 GitHub、代码评审和命令行中直接查看。Excel 原表仍保留用于表格导入和归档。

## 用例库

| 所属模块（对应需求） | 用例标题 | 前置条件 | 步骤 | 预期 | 关键词 | 优先级 | 用例类型 | 适用阶段 | 测试目的 | 体系结构 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-01 /sys 与设备节点存在 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c01_sysfs_entry_exists --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c01_sysfs_entry_exists ... # probe: watchdogN identity="..." options=0x.... timeout=Some(...) nowayout=Some(false)<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证 watchdog 设备能被 sysfs 与 devfs 正确枚举。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-02 sysfs 与 ioctl identity 一致 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c02_identity_consistent --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c02_identity_consistent ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证 sysfs identity 与 WDIOC_GETSUPPORT 返回的 identity 保持一致。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-03 options 声明 KEEPALIVEPING | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c03_options_advertise_keepaliveping --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c03_options_advertise_keepaliveping ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证驱动声明支持 WDIOC_KEEPALIVE 所需能力位。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-04 默认 timeout 合理 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c04_default_timeout_in_range --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c04_default_timeout_in_range ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证默认超时时间在可接受范围内，避免异常配置。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-05 WDIOC_KEEPALIVE 可用 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c05_keepalive_ioctl_works --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c05_keepalive_ioctl_works ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证 ioctl 喂狗路径能正常执行。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-06 Magic Close 周期安全 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c06_magic_close_cycle --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c06_magic_close_cycle ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证写入 magic V 后关闭设备不会误触发 watchdog。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-07 SETTIMEOUT 往返一致 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c07_set_timeout_round_trip --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c07_set_timeout_round_trip ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证设置 timeout 后内核返回值与 sysfs 回读一致。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-08 SETTIMEOUT(0) 拒绝 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c08_set_timeout_zero_rejected --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c08_set_timeout_zero_rejected ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证非法 timeout=0 不会被驱动接受。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-09 GETTIMELEFT 范围正确 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c09_timeleft_in_range --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c09_timeleft_in_range ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证剩余时间读取值处于当前 timeout 的合理范围内。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-10 Rust 生命周期日志存在 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_conformance-* c10_rust_lifecycle_log --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c10_rust_lifecycle_log ... # rust lifecycle: [INFO] [RUST] ...<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, conformance | P1 | 功能/接口 | 系统测试 | 验证待测驱动实际走 Rust probe/init 生命周期路径。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-EXT-01 write 字节喂狗 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_extended-* c_ext_01_write_byte_ping --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c_ext_01_write_byte_ping ... # probe: watchdogN identity="..." options=0x.... timeout=Some(...) nowayout=Some(false)<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, extended | P1 | 功能/压力 | 系统测试 | 验证 write(fd, byte) 传统喂狗路径区别于 WDIOC_KEEPALIVE 且可用。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-EXT-02 并发打开返回 busy | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_extended-* c_ext_02_concurrent_open_ebusy --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c_ext_02_concurrent_open_ebusy ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, extended | P1 | 功能/压力 | 系统测试 | 验证 watchdog core 单 opener 语义，防止多进程同时持有设备。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-EXT-03 超大 SETTIMEOUT 处理 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_extended-* c_ext_03_set_timeout_clamps_oversize --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c_ext_03_set_timeout_clamps_oversize ... # note: driver rejects oversize SETTIMEOUT outright 或成功 clamp 后回读一致<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, extended | P1 | 功能/压力 | 系统测试 | 验证超过硬件上限的 timeout 请求被拒绝或合理 clamp。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-EXT-04 bootstatus 可读 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_extended-* c_ext_04_bootstatus_readable --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c_ext_04_bootstatus_readable ... # bootstatus = 0x0000<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, extended | P1 | 功能/压力 | 系统测试 | 验证 WDIOC_GETBOOTSTATUS 可读且返回位图合法。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | C-EXT-05 timeleft 随时间递减 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/common_extended-* c_ext_05_timeleft_progresses --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test c_ext_05_timeleft_progresses ... <br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, common, extended | P1 | 功能/压力 | 系统测试 | 验证 GETTIMELEFT 返回真实计时状态，而不是固定值。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-01 三驱动 identity 精确匹配 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_01_identity --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_01_identity --test-threads=1 --nocapture --include-ignored<br>   # softdog<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_01_identity --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test sbsa_01_identity ... <br>   ok<br>   # SP5100<br>   test sp5100_01_identity ... <br>   ok<br>   # softdog<br>   test softdog_01_identity ... <br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, sbsa_gwdt-rust, sp5100_tco-rust, softdog-rust | P1 | 功能/驱动专项 | 系统测试 | 验证三类 Rust watchdog 驱动各自上报的 identity 与预期驱动身份精确一致。 | aarch64/x86_64；覆盖 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-02 三驱动 options 能力位 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_02_options_bitmap --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_02_options_bitmap --test-threads=1 --nocapture --include-ignored<br>   # softdog<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_02_options_advertised --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test sbsa_02_options_bitmap ... 要求 SETTIMEOUT/KEEPALIVEPING/MAGICCLOSE/CARDRESET<br>   ok<br>   # SP5100<br>   test sp5100_02_options_bitmap ... 要求 SETTIMEOUT/KEEPALIVEPING/MAGICCLOSE<br>   ok<br>   # softdog<br>   test softdog_02_options_advertised ... 要求 KEEPALIVEPING/MAGICCLOSE/SETTIMEOUT<br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, options, capabilities | P1 | 功能/驱动专项 | 系统测试 | 验证三类驱动声明的 watchdog 能力位符合各自设计预期。 | aarch64/x86_64；覆盖 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-03 三驱动 Rust 初始化日志 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_03_init_log_format --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_03_dmesg_init_log --test-threads=1 --nocapture --include-ignored<br>   # softdog<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_03_dmesg_init_log --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test sbsa_03_init_log_format ... Initialized with 10s timeout @ 50000000 Hz, action=0.<br>   ok<br>   # SP5100<br>   test sp5100_03_dmesg_init_log ... [RUST] Initialized. heartbeat=60 sec (nowayout=0)<br>   ok<br>   # softdog<br>   test softdog_03_dmesg_init_log ... # softdog init line: [INFO] [RUST] softdog initialized: soft_margin=60s soft_panic=0 nowayout=0<br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, dmesg, init log | P1 | 功能/驱动专项 | 系统测试 | 验证三类驱动均输出可识别的 Rust 初始化日志，确保加载路径和日志格式可回归检查。 | aarch64/x86_64；覆盖 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-04 三驱动连续喂狗 2 倍 timeout | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_ext_01_continuous_feed --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_ext_01_continuous_feed --test-threads=1 --nocapture --include-ignored<br>   # softdog<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_ext_01_continuous_feed --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test sbsa_ext_01_continuous_feed ... # feeding watchdog for 20s (timeout=10, ping every 5s)；# survived 4 pings over 20s<br>   ok<br>   # SP5100<br>   test sp5100_ext_01_continuous_feed ... # feeding watchdog for 120s (timeout=60, ping every 30s)；# survived 4 pings over 120s<br>   ok<br>   # softdog<br>   test softdog_ext_01_continuous_feed ... # feeding softdog for 120s (timeout=60, ping every 30s)；# survived 4 pings over 120s<br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, keepalive, continuous feed | P1 | 功能/压力 | 系统测试 | 验证三类驱动在 2 倍 timeout 时间窗口内持续喂狗时系统保持存活。 | aarch64/x86_64；覆盖 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-05 三驱动 write-byte ping | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_ext_02_write_byte_ping --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_ext_02_write_byte_ping --test-threads=1 --nocapture --include-ignored<br>   # softdog<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_ext_02_write_byte_ping --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test sbsa_ext_02_write_byte_ping ... <br>   ok<br>   # SP5100<br>   test sp5100_ext_02_write_byte_ping ... <br>   ok<br>   # softdog<br>   test softdog_ext_02_write_byte_ping ... <br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, write ping | P1 | 功能/接口 | 系统测试 | 验证三类驱动均支持通过 write(fd, byte) 触发喂狗。 | aarch64/x86_64；覆盖 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-06 三驱动 SETTIMEOUT 矩阵 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_ext_03_settimeout_matrix --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_ext_03_settimeout_matrix --test-threads=1 --nocapture --include-ignored<br>   # softdog<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_ext_03_settimeout_matrix --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test sbsa_ext_03_settimeout_matrix ... 设置 1/5/10/30/60/80 秒，返回值与 sysfs 回读一致<br>   ok<br>   # SP5100<br>   test sp5100_ext_03_settimeout_matrix ... 设置 1/5/10/30/60/80 秒，返回值与 sysfs 回读一致<br>   ok<br>   # softdog<br>   test softdog_ext_03_settimeout_matrix ... 设置 1/5/10/60/600 秒，返回值与 sysfs 回读一致<br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, SETTIMEOUT | P1 | 功能/接口 | 系统测试 | 验证三类驱动在多组 timeout 值下的设置结果与 sysfs 回读一致。 | aarch64/x86_64；覆盖 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-07 三驱动 DISABLE/ENABLE 选项 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_ext_04_setoptions_disable_enable --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_ext_04_setoptions_disable_enable --test-threads=1 --nocapture --include-ignored<br>   # softdog<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_ext_04_setoptions_disable_enable --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test sbsa_ext_04_setoptions_disable_enable ... DISABLE 后 state=inactive；ENABLE 后 state=active<br>   ok<br>   # SP5100<br>   test sp5100_ext_04_setoptions_disable_enable ... DISABLE 后 state=inactive；ENABLE 后 state=active<br>   ok<br>   # softdog<br>   test softdog_ext_04_setoptions_disable_enable ... DISABLE 后 state=inactive；ENABLE 后 state=active<br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, SETOPTIONS, start, stop | P1 | 功能/接口 | 系统测试 | 验证三类驱动通过 WDIOC_SETOPTIONS 执行 stop/start 后状态转换正确。 | aarch64/x86_64；覆盖 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-08 三驱动无 V 关闭保持运行 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_ext_05_close_without_v_keeps_running --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_ext_05_close_without_v_keeps_running --test-threads=1 --nocapture --include-ignored<br>   # softdog<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_ext_05_close_without_v_keeps_running --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test sbsa_ext_05_close_without_v_keeps_running ... watchdog: watchdog0: watchdog did not stop!<br>   ok<br>   # SP5100<br>   test sp5100_ext_05_close_without_v_keeps_running ... watchdog: watchdog0: watchdog did not stop!<br>   ok<br>   # softdog<br>   test softdog_ext_05_close_without_v_keeps_running ... watchdog: watchdog1: watchdog did not stop!<br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, magic close, no V | P1 | 功能/接口 | 系统测试 | 验证三类驱动在未写 magic V 关闭时保持 watchdog armed，符合 watchdog daemon 崩溃时应触发复位的语义。 | aarch64/x86_64；覆盖 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-09 三驱动 rmmod/modprobe 单次循环 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_ext_99_modprobe_cycle --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_ext_99_modprobe_cycle --test-threads=1 --nocapture --include-ignored<br>   # softdog<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_ext_99_modprobe_cycle --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test sbsa_ext_99_modprobe_cycle ... # rmmod sbsa_gwdt-rust；# modprobe sbsa_gwdt-rust<br>   ok<br>   # SP5100<br>   test sp5100_ext_99_modprobe_cycle ... # rmmod sp5100_tco-rust；# modprobe sp5100_tco-rust<br>   ok<br>   # softdog<br>   test softdog_ext_99_modprobe_cycle ... # rmmod softdog-rust；# modprobe softdog-rust<br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, rmmod, modprobe, lifecycle | P1 | 功能/稳定性 | 系统测试 | 验证三类驱动可完成一次卸载与重新加载，设备重新出现且 identity 保持一致。 | aarch64/x86_64；覆盖 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | SBSA-S-04 clock 频率合理 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 加载 sbsa_gwdt-rust nowayout=0，目标 identity 为 SBSA Generic Watchdog。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_04_clk_plausible --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test sbsa_04_clk_plausible ... Initialized with 10s timeout @ 50000000 Hz, action=0.<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, sbsa_gwdt-rust, clock | P1 | 功能/驱动专项 | 系统测试 | 验证 SBSA arch_timer 频率解析在合理范围。 | aarch64；N80；SBSA Generic Watchdog |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | SBSA-EXT-06 dmesg version 一致 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 加载 sbsa_gwdt-rust nowayout=0，目标 identity 为 SBSA Generic Watchdog。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_ext_06_dmesg_version_consistent --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test sbsa_ext_06_dmesg_version_consistent ... # detected SBSA GWDT version = 0<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, sbsa_gwdt-rust, version | P1 | 功能/驱动专项 | 系统测试 | 验证 SBSA W_IIDR/version 读取与日志解析。 | aarch64；N80；SBSA Generic Watchdog |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | SP5100-S-04 芯片组识别 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 加载 sp5100_tco-rust nowayout=0，目标 identity 为 SP5100 TCO timer。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_04_chipset_detected --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test sp5100_04_chipset_detected ... # detected SP5100 chipset layout = EFCH<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, sp5100_tco-rust, chipset | P1 | 功能/驱动专项 | 系统测试 | 验证 SP5100/SB800/EFCH 芯片布局探测逻辑。 | x86_64；Hygon；SP5100 TCO timer |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | SP5100-EXT-06 action 日志一致 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 加载 sp5100_tco-rust nowayout=0，目标 identity 为 SP5100 TCO timer。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_ext_06_dmesg_action_consistent --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test sp5100_ext_06_dmesg_action_consistent ... # detected SP5100 action = POWEROFF<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, sp5100_tco-rust, action | P1 | 功能/驱动专项 | 系统测试 | 验证 SP5100 action 参数和寄存器配置路径。 | x86_64；Hygon；SP5100 TCO timer |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | SOFTDOG-EXT-06 模块参数日志可见 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 加载 softdog-rust nowayout=0，目标 identity 为 Software Watchdog (Rust)。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_ext_06_module_param_visible --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test softdog_ext_06_module_param_visible ... # detected soft_margin=60s nowayout=0<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, softdog-rust, module param | P1 | 功能/驱动专项 | 系统测试 | 验证 softdog probe 参数日志可见且参数值合法。 | aarch64/x86_64；N80/Hygon；Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | DRV-10 SBSA/SP5100 日志正则自检 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 三类待测驱动与 identity 对应关系如下：<br>   N80/aarch64: sudo modprobe sbsa_gwdt-rust nowayout=0；identity=SBSA Generic Watchdog。<br>   Hygon/x86_64: sudo modprobe sp5100_tco-rust nowayout=0；identity=SP5100 TCO timer。<br>   N80 或 Hygon: sudo modprobe softdog-rust nowayout=0；identity=Software Watchdog (Rust)。<br>6. 对三类 identity 分别执行同一用例；每个 identity 的结果独立判定。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br><br>3. 对三类驱动分别执行对应二进制和测试函数：<br>   # SBSA<br>   WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* regex_lite_self_check::matches_init_log_pattern --test-threads=1 --nocapture --include-ignored<br>   # SP5100<br>   WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* regex_lite_self_check::matches_init_log_pattern --test-threads=1 --nocapture --include-ignored<br>4. 三类驱动均需查看 tests.log 中对应 test 行和 test result 汇总；任一 identity 失败则该用例失败。 | 1. 三类驱动分别出现类似输出，且均为 ok：<br>   # SBSA<br>   test regex_lite_self_check::matches_init_log_pattern ... <br>   ok<br>   # SP5100<br>   test regex_lite_self_check::matches_init_log_pattern ... <br>   ok<br>2. 每个驱动对应测试二进制汇总均为 test result: ok，failed 为 0。 | watchdog, regex, test helper | P2 | 测试工具自检 | 系统测试 | 验证 per-driver 日志匹配辅助规则自身正确，避免日志格式测试误判。 | aarch64/x86_64；SBSA 与 SP5100 |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | GC-01 打开设备并检视信息 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 运行 gc_test 二进制；目标 identity 可为 SBSA Generic Watchdog、SP5100 TCO timer 或 Software Watchdog (Rust)。<br>6. GC-03 为破坏性复位用例，只有在明确设置 WATCHDOG_LAB_DANGEROUS=YES_REALLY 时执行。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/gc_test-* gc_01_device_info --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test gc_01_device_info ... # 测试设备：watchdogN identity="..." options=0x.... timeout=Some(...) nowayout=Some(false)<br>   # sysfs 信息：identity="..." timeout=... nowayout=false state="inactive"<br>   # ioctl 信息：identity="..." options=0x.... firmware_version=0<br>   # GC-01 通过：设备信息读取正常<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, gc_test, QA | P0 | 端到端 | 系统测试 | 端到端验证设备基础信息、sysfs 与 ioctl 信息一致性。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | GC-02 设置 timeout 并周期喂狗 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 运行 gc_test 二进制；目标 identity 可为 SBSA Generic Watchdog、SP5100 TCO timer 或 Software Watchdog (Rust)。<br>6. GC-03 为破坏性复位用例，只有在明确设置 WATCHDOG_LAB_DANGEROUS=YES_REALLY 时执行。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/gc_test-* gc_02_feed_within_timeout --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test gc_02_feed_within_timeout ... # 阶段 1：SETTIMEOUT 多值往返测试<br>   #   SETTIMEOUT(5) -> 5s ✓<br>   #   SETTIMEOUT(10) -> 10s ✓<br>   #   SETTIMEOUT(30) -> 30s ✓<br>   # 阶段 2：连续喂狗 120s（timeout=60s，每 30s ping 一次）<br>   # GC-02 通过：累计喂狗 4 次，120s 内系统存活<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, gc_test, QA | P0 | 端到端 | 系统测试 | 端到端验证 timeout 配置和窗口内持续喂狗能力。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | GC-03 超时未喂狗触发复位 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 运行 gc_test 二进制；目标 identity 可为 SBSA Generic Watchdog、SP5100 TCO timer 或 Software Watchdog (Rust)。<br>6. GC-03 为破坏性复位用例，只有在明确设置 WATCHDOG_LAB_DANGEROUS=YES_REALLY 时执行。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/gc_test-* gc_03_no_feed_reboot --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。<br>5. 如需执行破坏性复位验证，命令需显式增加授权变量：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" WATCHDOG_LAB_DANGEROUS=YES_REALLY sudo -E /tmp/watchdog-test/gc_test-* gc_03_no_feed_reboot --test-threads=1 --nocapture --include-ignored | 1. tests.log 中出现类似输出：<br>   test gc_03_no_feed_reboot ... # SKIP: lab-CI test requires WATCHDOG_LAB_DANGEROUS=YES_REALLY (this test will REBOOT the machine on success)<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, gc_test, QA | P1 | 端到端 | 系统测试 | 验证不喂狗时 watchdog 能触发真实复位；非 lab 模式必须安全跳过。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |
| 银河麒麟桌面操作系统-终端模块研发项目-内核-V11-迭代4-623549 | GC-04 未使能状态下 10 次卸载/加载 | 1. 测试控制端位于 watchdog-test 项目根目录。<br>2. 测试机已运行待测 Rust watchdog 内核，SSH 可达，且测试用户已配置 sudo NOPASSWD，执行 sudo 不需要输入密码。<br>3. 测试机存在 /sys/class/watchdog/watchdogN 与 /dev/watchdogN；deploy.sh 会自动按架构构建并推送测试二进制到 /tmp/watchdog-test。<br>4. autonomous 模式会以 nowayout=0 加载待测模块，避免非破坏性用例触发不可停止复位。<br>5. 运行 gc_test 二进制；目标 identity 可为 SBSA Generic Watchdog、SP5100 TCO timer 或 Software Watchdog (Rust)。<br>6. GC-03 为破坏性复位用例，只有在明确设置 WATCHDOG_LAB_DANGEROUS=YES_REALLY 时执行。 | 1. 在项目根目录执行归档测试命令：<br>   ./scripts/capture-run.sh <TARGET><br>   其中 <TARGET> 可为 N80 或 Hygon。<br>2. capture-run.sh 会调用 deploy.sh，deploy.sh 自动执行：<br>   ssh <TARGET> uname -m<br>   ./scripts/build.sh <aarch64\|x86_64><br>   ssh <TARGET> "sudo modprobe <module> nowayout=0"<br>   scp target/<triple>/release/deps/<test-binary> <TARGET>:/tmp/watchdog-test/<br>3. 也可在测试机上单独复现该用例：<br>   WATCHDOG_TEST_IDENTITY="<目标 identity>" sudo -E /tmp/watchdog-test/gc_test-* gc_04_modprobe_cycle_x10 --test-threads=1 --nocapture --include-ignored<br>4. 查看 logs/<run>/tests.log 中对应 test 行、probe 输出和 test result 汇总。 | 1. tests.log 中出现类似输出：<br>   test gc_04_modprobe_cycle_x10 ... # 对内核模块 "<module>"（identity="..."）反复加载/卸载 10 次<br>   # 第  1/10 轮通过<br>   ...<br>   # 第 10/10 轮通过<br>   # GC-04 通过：10/10 轮完成 —— 无残留 fd 占用、identity 保持一致、每次 reload 后 ops 仍可用<br>   ok<br>2. 当前测试二进制汇总为 test result: ok，failed 为 0。 | watchdog, gc_test, QA | P0 | 端到端/稳定性 | 系统测试 | 端到端验证模块反复卸载/加载后的设备重建、引用计数和 ops 绑定稳定性。 | aarch64/x86_64；适用于 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust) |

## 执行指南

| 阶段 | 操作 | 命令/文件 | 说明/验证点 |
| --- | --- | --- | --- |
| 0. 前置条件 | 明确测试覆盖目标 | 覆盖 aarch64: 需要 N80/aarch64 测试机或等价 ARM64 测试环境<br>覆盖 x86_64: 需要 Hygon/x86_64 测试机或等价 x86_64 测试环境 | 交叉编译只能生成另一架构的测试二进制，不能替代真实目标机执行。要声明某架构测试通过，必须在该架构机器上运行 deploy/capture 并检查日志。 |
| 0. 前置条件 | 安装 Rust/rustup | curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \| sh<br># 按提示选择默认安装即可；离线环境可使用内网 rustup 镜像或预装包。 | 在编译控制端执行。watchdog-test 使用 cargo 编译 Rust 测试二进制；没有 rustup/cargo 时 build.sh 无法工作。 |
| 0. 前置条件 | 加载 Rust 环境变量 | source "$HOME/.cargo/env"<br># 建议加入 shell 启动文件：<br>echo 'source "$HOME/.cargo/env"' >> ~/.bashrc | 新装 rustup 后当前 shell 需要加载 ~/.cargo/env，否则可能提示 cargo/rustup: command not found。 |
| 0. 前置条件 | 校验 Rust 工具链 | rustc --version<br>cargo --version<br>rustup --version<br>rustup show | 确认 rustc、cargo、rustup 都可用，并确认 default toolchain 已安装。若命令不存在，先检查 PATH 是否包含 ~/.cargo/bin。 |
| 0. 前置条件 | 准备交叉编译 Rust target | rustup target add aarch64-unknown-linux-musl<br>rustup target add x86_64-unknown-linux-musl<br>rustup target list --installed \| rg "aarch64-unknown-linux-musl\|x86_64-unknown-linux-musl" | 如果编译控制端只有 ARM 或只有 x86，也可以同时安装两个 Rust target，用同一份仓库交叉编译出两套静态测试二进制。 |
| 0. 前置条件 | 准备系统构建工具 | sudo apt-get install -y build-essential pkg-config clang lld musl-tools<br># 若系统不是 Debian/Ubuntu/Kylin apt 系，安装等价的 C 编译器、链接器和 musl 工具链即可。 | 多数 Rust 依赖可直接交叉编译；若遇到 linker not found、cc failed、cannot find crt*.o 等错误，优先补齐 clang/lld/musl 或对应发行版的交叉工具链包。 |
| 0. 前置条件 | 确认目标机运行待测内核和模块 | ssh <TARGET> "uname -a"<br>ssh <TARGET> "find /lib/modules/$(uname -r)/kernel/drivers/watchdog -name "*.ko*" \| rg "sbsa_gwdt\|sp5100\|softdog"" | 测试机必须运行包含待测 Rust watchdog 模块的内核。只有二进制编译成功但目标机没有对应模块时，deploy.sh 会无法加载该驱动或只能覆盖 softdog。 |
| 1. 获取仓库 | 克隆测试仓库 | git clone https://github.com/TomGoh/watchdog-test.git<br>cd watchdog-test | 在测试控制端执行。后续所有命令默认在 watchdog-test 项目根目录运行。 |
| 1. 获取仓库 | 更新已有仓库 | git pull --ff-only | 如果本地已经有仓库，用该命令同步 GitHub 最新代码；若存在本地修改，先确认不要覆盖未保存内容。 |
| 2. 准备测试机访问 | 确认 SSH 可达 | ssh <TARGET> hostname<br>ssh <TARGET> uname -a | <TARGET> 可为 SSH alias、IP 或 user@host。N80/Hygon 可写入 ~/.ssh/config 作为别名。 |
| 2. 准备测试机访问 | 配置免密 sudo | echo "$(whoami) ALL=(ALL) NOPASSWD: ALL" \| sudo tee /etc/sudoers.d/$(whoami)-nopasswd<br>sudo chmod 0440 /etc/sudoers.d/$(whoami)-nopasswd<br>sudo visudo -c -f /etc/sudoers.d/$(whoami)-nopasswd | 在测试机上配置。deploy.sh 需要 sudo 打开 /dev/watchdogN、读取 dmesg/kmsg、modprobe/rmmod；sudo 不应提示密码。 |
| 2. 准备测试机访问 | 确认测试机 watchdog 状态 | ssh <TARGET> 'for w in /sys/class/watchdog/watchdog*; do [ -e "$w" ] \|\| continue; echo "$w"; cat "$w/identity" 2>/dev/null; cat "$w/timeout" 2>/dev/null; cat "$w/nowayout" 2>/dev/null; done' | 确认目标机器当前是否已有 watchdog，记录 identity、timeout、nowayout。 |
| 3. 编译 | 安装 Rust target | rustup target add aarch64-unknown-linux-musl<br>rustup target add x86_64-unknown-linux-musl | N80/aarch64 使用 aarch64-unknown-linux-musl；Hygon/x86_64 使用 x86_64-unknown-linux-musl。deploy.sh 在缺少缓存二进制时也会自动调用 build.sh。 |
| 3. 编译 | 手动编译 aarch64 测试二进制 | ./scripts/build.sh aarch64 | 输出位于 target/aarch64-unknown-linux-musl/release/deps/，包含 common_conformance、common_extended、gc_test、sbsa_gwdt、softdog 等测试二进制。 |
| 3. 编译 | 手动编译 x86_64 测试二进制 | ./scripts/build.sh x86_64 | 输出位于 target/x86_64-unknown-linux-musl/release/deps/，包含 common_conformance、common_extended、gc_test、sp5100_tco、softdog 等测试二进制。 |
| 4. 自动部署并测试 | 运行 autonomous 全量测试 | ./scripts/deploy.sh <TARGET> | 脚本会 ssh 到目标机识别架构，按架构加载模块：aarch64 加载 sbsa_gwdt-rust softdog-rust；x86_64 加载 sp5100_tco-rust softdog-rust；随后推送二进制并逐个 identity 执行测试。 |
| 4. 自动部署并测试 | 归档一次完整测试 | ./scripts/capture-run.sh <TARGET> | 推荐使用该命令。它会调用 deploy.sh，并把 meta、tests.log、dmesg-pre/post/delta、meta-post 写入 logs/<日期>-<目标>-autonomous-<时间>/。 |
| 4. 自动部署并测试 | ARM64/N80 完整 terminal 示例 | # 编译控制端执行<br>git clone https://github.com/TomGoh/watchdog-test.git<br>cd watchdog-test<br>source "$HOME/.cargo/env"<br>rustup target add aarch64-unknown-linux-musl<br>rustup target add x86_64-unknown-linux-musl<br><br># 确认 ARM64 测试机可访问、sudo 免密可用<br>ssh N80 "uname -m; uname -a"<br>ssh N80 "sudo -n true"<br>ssh N80 "sudo -n dmesg >/dev/null"<br><br># 可选：先手动编译 ARM64 测试二进制<br>./scripts/build.sh aarch64<br><br># 推荐：归档完整 autonomous 测试<br>./scripts/capture-run.sh N80<br><br># 查看最近一次 N80 结果<br>RUN=$(ls -td logs/*-N80-autonomous-* \| head -1)<br>rg "Testing identity\|test result:\|FAILED\|failures:" "$RUN/tests.log"<br>rg "RUST\|watchdog\|sbsa\|softdog\|set_timeout\|did not stop" "$RUN/dmesg-delta.log"<br>rg -i "panic\|oops\|bug\|call trace\|segfault" "$RUN/dmesg-delta.log" "$RUN/tests.log" | 覆盖 N80/aarch64 上的 SBSA Generic Watchdog 和 Software Watchdog (Rust)。预期 tests.log 所有非 lab 项 test result: ok，dmesg-delta.log 能看到 sbsa_gwdt/softdog 的 [RUST] 生命周期、set_timeout、start/stop 等日志，异常关键字检查为空。 |
| 4. 自动部署并测试 | x86_64/Hygon 完整 terminal 示例 | # 编译控制端执行<br>git clone https://github.com/TomGoh/watchdog-test.git<br>cd watchdog-test<br>source "$HOME/.cargo/env"<br>rustup target add aarch64-unknown-linux-musl<br>rustup target add x86_64-unknown-linux-musl<br><br># 确认 x86_64 测试机可访问、sudo 免密可用<br>ssh Hygon "uname -m; uname -a"<br>ssh Hygon "sudo -n true"<br>ssh Hygon "sudo -n dmesg >/dev/null"<br><br># 可选：先手动编译 x86_64 测试二进制<br>./scripts/build.sh x86_64<br><br># 推荐：归档完整 autonomous 测试<br>./scripts/capture-run.sh Hygon<br><br># 查看最近一次 Hygon 结果<br>RUN=$(ls -td logs/*-Hygon-autonomous-* \| head -1)<br>rg "Testing identity\|test result:\|FAILED\|failures:" "$RUN/tests.log"<br>rg "RUST\|watchdog\|sp5100\|softdog\|set_timeout\|did not stop" "$RUN/dmesg-delta.log"<br>rg -i "panic\|oops\|bug\|call trace\|segfault" "$RUN/dmesg-delta.log" "$RUN/tests.log" | 覆盖 Hygon/x86_64 上的 SP5100 TCO timer 和 Software Watchdog (Rust)。Hygon 上 dmesg 通常需要 sudo，因此示例显式检查 sudo -n dmesg；预期 tests.log 全部通过，dmesg-delta.log 能看到 sp5100/softdog 的 [RUST] 日志。 |
| 4. 自动部署并测试 | 执行破坏性 lab 测试 | ./scripts/capture-run.sh <TARGET> --lab <module><br># 示例：<br>./scripts/capture-run.sh N80 --lab sbsa_gwdt-rust<br>./scripts/capture-run.sh Hygon --lab sp5100_tco-rust | lab 模式可能导致机器复位。只有需要验证“不喂狗真实复位”或 magic close 失败场景时执行。 |
| 5. 命令行单项测试 | 查看远端已推送二进制 | ssh <TARGET> "ls -l /tmp/watchdog-test/" | deploy.sh/capture-run.sh 会清理并推送需要的二进制到 /tmp/watchdog-test。单项测试前先确认文件名。 |
| 5. 命令行单项测试 | 运行 common conformance 单项 | WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/common_conformance-* c01_sysfs_entry_exists --test-threads=1 --nocapture --include-ignored | 在测试机上执行。可将 identity 替换为 SP5100 TCO timer 或 Software Watchdog (Rust)。 |
| 5. 命令行单项测试 | 运行 SBSA 专项单项 | WATCHDOG_TEST_IDENTITY="SBSA Generic Watchdog" sudo -E /tmp/watchdog-test/sbsa_gwdt-* sbsa_ext_01_continuous_feed --test-threads=1 --nocapture --include-ignored | 适用于 N80/aarch64 的 SBSA Generic Watchdog。预期看到 feeding watchdog for 20s、survived 4 pings over 20s。 |
| 5. 命令行单项测试 | 运行 SP5100 专项单项 | WATCHDOG_TEST_IDENTITY="SP5100 TCO timer" sudo -E /tmp/watchdog-test/sp5100_tco-* sp5100_ext_01_continuous_feed --test-threads=1 --nocapture --include-ignored | 适用于 Hygon/x86_64 的 SP5100 TCO timer。预期看到 feeding watchdog for 120s、survived 4 pings over 120s。 |
| 5. 命令行单项测试 | 运行 softdog 专项单项 | WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/softdog-* softdog_ext_06_module_param_visible --test-threads=1 --nocapture --include-ignored | 适用于 N80 或 Hygon 的 softdog-rust。预期看到 detected soft_margin=60s nowayout=0。 |
| 5. 命令行单项测试 | 运行 GC 端到端单项 | WATCHDOG_TEST_IDENTITY="Software Watchdog (Rust)" sudo -E /tmp/watchdog-test/gc_test-* gc_02_feed_within_timeout --test-threads=1 --nocapture --include-ignored | 验证 SETTIMEOUT 多值往返和 2 倍 timeout 周期内持续喂狗。 |
| 5. 命令行单项测试 | 运行 GC-03 破坏性复位 | WATCHDOG_TEST_IDENTITY="<identity>" WATCHDOG_LAB_DANGEROUS=YES_REALLY sudo -E /tmp/watchdog-test/gc_test-* gc_03_no_feed_reboot --test-threads=1 --nocapture --include-ignored | 该命令成功时机器会复位，进程通常不会正常返回。仅在实验室复位验证阶段执行。 |
| 6. 验证 stdout 结果 | 检查 tests.log 总体结果 | rg "test result:\|FAILED\|failures:" logs/<run>/tests.log | 所有非 lab 用例应为 test result: ok，failed 为 0。若出现 FAILED 或 failures，需要定位具体 test 行。 |
| 6. 验证 stdout 结果 | 检查测试 identity | rg "Testing identity\|probe:\|测试设备" logs/<run>/tests.log | 确认实际测试到了预期 identity，例如 SBSA Generic Watchdog、SP5100 TCO timer、Software Watchdog (Rust)。 |
| 6. 验证 stdout 结果 | 检查典型通过输出 | rg "survived\|GC-.*通过\|detected\|bootstatus\|rust lifecycle" logs/<run>/tests.log | 连续喂狗、GC、chipset/action/version/module-param 等关键输出应能对应到用例表中的预期样例。 |
| 7. 验证 dmesg | 查看新增内核日志 | sed -n '1,200p' logs/<run>/dmesg-delta.log<br># 如需直接在测试机查看：<br>ssh <TARGET> "sudo -n dmesg \| grep -iE 'RUST\|watchdog\|softdog\|sp5100\|sbsa'" | dmesg-delta.log 是 pre/post 的差异，优先看这里。capture-run.sh 使用 sudo -n dmesg 采集，适配 Hygon 这类限制普通用户直接读取 dmesg 的机器。 |
| 7. 验证 dmesg | 筛选 watchdog/Rust 关键日志 | rg "RUST\|watchdog\|sbsa\|sp5100\|softdog\|Initialized\|set_timeout\|did not stop" logs/<run>/dmesg-delta.log | 用于确认模块 probe、timeout 设置、start/stop、无 V 关闭等内核侧行为。 |
| 7. 验证 dmesg | 检查异常日志 | rg -i "panic\|oops\|bug\|failed\|error\|call trace\|segfault" logs/<run>/dmesg-delta.log logs/<run>/tests.log | 正常 autonomous 测试不应出现 panic/Oops/Call Trace。若出现，需要结合具体用例和模块生命周期定位。 |
| 8. 验证 meta | 查看测试前后模块状态 | cat logs/<run>/meta.txt<br>cat logs/<run>/meta-post.txt | meta.txt 记录测试前 uname、uptime、lsmod、watchdog inventory；meta-post.txt 记录测试后状态，用于确认模块是否遗留、设备是否恢复。 |
| 8. 验证 meta | 检查测试机仍可访问 | ssh <TARGET> uptime<br>ssh <TARGET> "ls /sys/class/watchdog/" | 非破坏性 autonomous 测试结束后机器应仍在线；watchdog 设备应处于可枚举状态或按 cleanup 预期卸载。 |
| 9. 常见问题 | sudo 需要密码 | ssh <TARGET> "sudo -n true" | 该命令应无输出且返回 0。若失败，说明 sudo NOPASSWD 未配置好，deploy/capture 可能卡住或失败；dmesg-pre/post 也可能只保存 sudo 失败信息。 |
| 9. 常见问题 | 找不到测试二进制 | ls target/<triple>/release/deps/<br>./scripts/build.sh <aarch64\|x86_64> | 如果 deploy.sh 提示 No test binaries match pattern，先重新编译对应架构。 |
| 9. 常见问题 | 没有 watchdog 设备 | ssh <TARGET> "sudo modprobe <module> nowayout=0; ls /sys/class/watchdog/; dmesg \| tail -n 80" | 确认模块是否适用于当前硬件。x86_64 通常测 sp5100_tco-rust，aarch64 通常测 sbsa_gwdt-rust，softdog-rust 两边都可测。 |
| 9. 常见问题 | 单项测试找错设备 | WATCHDOG_TEST_IDENTITY="<准确 identity>" sudo -E /tmp/watchdog-test/<binary>-* <test> --test-threads=1 --nocapture --include-ignored | 多 watchdog 同时存在时必须设置 WATCHDOG_TEST_IDENTITY，否则 pick_watchdog 可能选择非预期设备。 |
